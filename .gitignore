/target
/target2
*.part?
/scratch

# Kernel module build artifacts + VM harness output
kernel/**/*.ko
kernel/**/*.o
kernel/**/*.mod
kernel/**/*.mod.c
kernel/**/.*.cmd
kernel/**/Module.symvers
kernel/**/modules.order
kernel/test/out/

# Agent worktrees and session scratch — never part of the tree
/.claude/

# **Every .env, not just `.env`.** The bare name is a literal: it does not
# match `.env.local`, which is where a summariser key actually lands. An
# example file stays committable.
.env*
!.env.example
